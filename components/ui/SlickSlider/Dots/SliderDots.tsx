function SliderDots({ dots }: { dots: React.ReactNode }) {
  return (
    <div className="">
      <ul className="list-none embla-slider-dots">{dots}</ul>
    </div>
  );
}

export default SliderDots;
